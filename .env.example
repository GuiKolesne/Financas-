# Copie este arquivo para .env.local e preencha com os valores do seu projeto
# Supabase (Project Settings -> API). O .env.local nunca vai para o git.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
